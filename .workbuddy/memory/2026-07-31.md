# 2026-07-31

- 完成 FerroxPlorer v0.4.6 性能与发布改造：将 WPD/磁盘/回收站轮询移出 Slint UI 线程，侧栏与状态栏改读后台缓存；文件选中热路径不再同步读取创建时间、默认程序、所有者、ACL 和数字签名，属性对话框打开时再惰性加载。
- 缩略图请求由 256px 降至 128px，具体路径缓存上限由 500 降至 256，降低常驻内存。
- 新增 `.cargo/config.toml`，Windows MSVC 静态链接 CRT；安装器要求 Windows 10 1809+ x64。
- GitHub Actions Release 工作流增加 `--locked` 测试/构建及 dumpbin 运行时依赖门禁，随后由 Inno Setup 打包并创建 Release。
- 本地验证 `cargo test --locked --all-targets`：42 passed；`cargo build --locked --release` 成功。
- 提交 `0e9192f` 已推送 main，标签 `v0.4.6` 已推送，Actions run `30603271930` 正在执行发布。
- 修复 COM 套间冲突崩溃：`list_devices_win()` 中的 `CoInitializeEx(COINIT_MULTITHREADED)` 经 `virtualfs::resolve → list_devices` 在 UI 线程执行时，将 UI 线程初始化为 MTA，导致 winit `OleInitialize(STA)` 报 `RPC_E_CHANGED_MODE` 崩溃。修复方式：从 `list_devices_win` 移除 COM 初始化，改到后台轮询线程入口处执行；UI 线程 COM 由 winit 以 STA 初始化。
- Debug 和 Release 版本均验证可正常启动（无 panic）。42 测试全通过。
- 重新提交 `2e6d61b` 并覆盖推送 `v0.4.6` 标签，Actions run `30605815724` 正在执行发布。
- 完成视频 Quick Look 控制栏交互改造：播放/暂停、重播、进度点击拖动、静音按钮；Rust 轮询光标，移动显示、静止 5 秒隐藏，并动态缩放 MFPlay 子窗口为控制栏让位。
- 修复 `GetMute` 的 Windows BOOL 转换与 `ScreenToClient` HWND 类型错误；同步标题栏高度到右键菜单坐标（48px→40px），并让标题栏样式移除 `WS_CAPTION|WS_SYSMENU` 可重复应用，避免 DWM 原生按钮与自绘按钮重复。
- UI 修复验证：`cargo check --locked --all-targets` 通过，`cargo test --locked --all-targets` 通过（42 passed），`cargo build --locked --release` 通过；Debug/Release 启动 8 秒均无 panic，进程因测试超时被正常结束。
- UI 修复提交 `9b4efe4` 已推送 `main`，远程 `v0.4.6` 标签已更新到该提交。GitHub Actions run `30610470554` 全部成功（含测试、Release 构建、静态 CRT 依赖门禁、Inno Setup 打包和 Release 上传）；最新资产 `FerroxPlorer-Setup-0.4.6.exe`，大小 10,564,221 字节，SHA-256 `59699089c3d24260148b4122deb1713f05a97585dfe9d8167506e5c72f695bb2`。
- v0.4.7 已完成 GitHub Actions 发布：提交 `5c91b47`、标签 `v0.4.7`，Actions run `30613044791` 全部成功（测试、Release 构建、静态 CRT 自包含依赖检查、Inno Setup 打包、Release 上传）。Release 地址：https://github.com/ling552/FerroxPlorer/releases/tag/v0.4.7；安装包 `FerroxPlorer-Setup-0.4.7.exe`，大小 10,560,350 字节，SHA-256 `0c3a8fe4cc3b2f6fb25ddfc2a8a14621174b0adec6aeb95eadf83a4dfc1c882f`。本地副本位于 `outputs/FerroxPlorer-Setup-0.4.7.exe`。
- 按参考图修正视频 Quick Look 控制栏：将横向布局改为起始对齐，进度轨道设置为可伸展并占据剩余宽度，固定时间显示宽度 50px；控制条高度及 Rust 原生视频让位高度从 44px 同步为 48px。保留鼠标移动显示、静止 5 秒隐藏逻辑。`cargo check --locked --all-targets`、42 项测试、`cargo build --locked --release` 均通过。
- 完成视频 Quick Look 浮层布局收尾：视频模式移除底部“按空格或 Esc”独立白色提示区，视频内容延伸至卡片底边；48px 原生控制栏以约 80% 不透明度覆盖在视频底部并横向铺满。鼠标在整个预览卡片（含标题栏/边带）内移动时显示，静止 5 秒隐藏，卡片外移动不唤醒。`cargo check --locked --all-targets` 通过，`cargo test --locked --all-targets` 42 项全通过；仅保留项目既有的 4 项非阻断 warning。
- 修复视频全屏重复 UI：彻底移除未使用的 Slint `VideoControlBar`，窗口与全屏仅保留 MFPlay 上方的一套原生半透明控制层；播放/暂停改为实心几何图形，重播改圆弧箭头，音量/静音改扬声器与声波/叉号矢量图标，并重排右侧时间与进度轨道。视频全屏时隐藏应用自绘标题栏按钮；全屏进入、退出及直接关闭预览后延迟重应用无边框窗口样式，防止 DWM 原生按钮重现。`cargo check --locked --all-targets`、42 项测试和 `cargo build --locked` 均通过，仅有既有 4 项非阻断 warning。
- 按新参考图再次重绘原生视频控制栏：暂停改 2px 空心圆角双竖条，播放改细线三角，重播改细圆弧箭头，音量改开放式扬声器与双声波，进度滑块改空心圆，轨道改细圆角线并调整控件间距。为彻底消除退出全屏时约 0.5 秒 Win7 样式系统按钮闪现，在主窗口 WNDPROC 的 `WM_STYLECHANGING` 中同步清除目标 `WS_CAPTION|WS_SYSMENU`，不再依赖事后延迟补刷。`cargo check --locked --all-targets`、42 项测试及 `cargo build --locked` 均通过，仅保留既有 4 项非阻断 warning。
- 用户反馈图标观感仍未明显改善；确认原因是 GDI 几何细线缺少抗锯齿。将播放/暂停、重播、音量/静音改为 `Segoe MDL2 Assets` 系统媒体字形，通过 `CreateFontW` + `ANTIALIASED_QUALITY` 绘制，与 Windows 系统播放器同源；保留空心圆进度滑块与细圆角轨道。`cargo check --locked --all-targets` 和 42 项测试均通过，仅保留既有 4 项非阻断 warning。
- 修复视频控制栏点击后的焦点与边框问题：原生浮层增加 `WS_EX_TOOLWINDOW|WS_EX_NOACTIVATE` 并在 `WM_MOUSEACTIVATE` 返回 `MA_NOACTIVATE`，使按钮可点击但不抢主 Slint 窗口的键盘焦点，空格仍可关闭预览；主窗口 WNDPROC 对 `WM_NCACTIVATE` 直接确认，阻止 DWM 因激活状态变化绘制原生活动边框。未采用事后 `SetActiveWindow/SetFocus`，避免触发额外非客户区重绘。`cargo check --locked --all-targets` 与 42 项测试均通过，仅保留既有 4 项非阻断 warning。
- FerroxPlorer v0.4.8 发布完成：提交 `201d5ac`、标签 `v0.4.8` 已推送；GitHub Actions run `30628643748` 全部成功（测试、Release 构建、静态 CRT 自包含依赖检查、Inno Setup 打包和 Release 上传）。Release：https://github.com/ling552/FerroxPlorer/releases/tag/v0.4.8；资产 `FerroxPlorer-Setup-0.4.8.exe`，大小 10,546,156 字节，SHA-256 `4bd33b55421e5fe3947b9de5b7ef8e80cdadec470f8ab8939144df7905a2f52d`。
